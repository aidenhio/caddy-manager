diff --git a/app.py b/app.py
index fa72444..5704ae4 100644
--- a/app.py
+++ b/app.py
@@ -613,18 +613,13 @@ def edit_block(filename):
     meta = read_metadata(filename, path)
     block_type = meta.get("type", "custom")
     old_hosts = meta.get("hosts", [])
-    # Custom blocks are edited through the same host-aware form as the other
-    # types as long as we actually found a host line to key off of -- a file
-    # with no recognizable header at all falls back to the plain raw editor.
-    structured = block_type in ("reverse_proxy", "redirect", "load_balancer") or \
-        (block_type == "custom" and bool(old_hosts))
     error = None
     raw_body_text = ""
     if block_type == "custom" and request.method == "GET":
         with open(path) as f:
             raw_body_text = extract_body(f.read())
 
-    if request.method == "POST" and structured:
+    if request.method == "POST":
         hosts = hosts_from_textarea(request.form.get("hosts", ""))
         content = None
         new_meta = None
@@ -697,26 +692,13 @@ def edit_block(filename):
             flash(f"Saved {filename}", "success")
             return redirect(url_for("sites"))
 
-    elif request.method == "POST":  # raw edit -- last resort for a genuinely unparseable file
-        content = request.form.get("content", "")
-        with open(path, "w") as f:
-            f.write(content)
-        write_metadata(filename, path, {"type": "custom", "hosts": extract_hosts(content)})
-        flash(f"Saved {filename}", "success")
-        return redirect(url_for("sites"))
-
-    if structured:
-        upstreams_text = "\n".join(meta.get("upstreams") or [])
-        hosts_text = "\n".join(meta.get("hosts") or [])
-        return render_template(
-            "block_form.html", mode="edit", block_type=block_type, filename=filename,
-            meta=meta, upstreams_text=upstreams_text, hosts_text=hosts_text,
-            raw_body_text=raw_body_text, error=error
-        )
-
-    with open(path, "r") as f:
-        content = f.read()
-    return render_template("edit.html", filename=filename, content=content)
+    upstreams_text = "\n".join(meta.get("upstreams") or [])
+    hosts_text = "\n".join(meta.get("hosts") or [])
+    return render_template(
+        "block_form.html", mode="edit", block_type=block_type, filename=filename,
+        meta=meta, upstreams_text=upstreams_text, hosts_text=hosts_text,
+        raw_body_text=raw_body_text, error=error
+    )
 
 
 @app.route("/toggle/<path:filename>", methods=["POST"])
diff --git a/templates/block_form.html b/templates/block_form.html
index 682a822..f2d104b 100644
--- a/templates/block_form.html
+++ b/templates/block_form.html
@@ -46,6 +46,7 @@
             <div class="form-control d-flex flex-wrap align-items-center gap-2" id="hosts-input">
               <input type="text" id="hosts-text-input" class="form-control-plaintext p-0 flex-grow-1" placeholder="app.example.com" autocomplete="off">
             </div>
+            <div class="invalid-feedback">At least one host is required.</div>
             <textarea name="hosts" id="hosts" hidden>{{ hosts_text }}</textarea>
           </div>
           <!-- END HOSTS FIELD -->
@@ -70,6 +71,8 @@
                 value = value.trim();
                 if (!value || currentHosts().includes(value)) return;
 
+                wrap.classList.remove('is-invalid');
+
                 const chip = document.createElement('span');
                 chip.className = 'host-chip badge bg-' + typeColor + '-lt d-inline-flex align-items-center gap-1';
                 chip.dataset.host = value;
@@ -118,8 +121,15 @@
               });
 
               wrap.addEventListener('click', () => textInput.focus());
-              wrap.closest('form').addEventListener('submit', () => {
+              wrap.closest('form').addEventListener('submit', (e) => {
                 if (textInput.value.trim()) addChip(textInput.value);
+                if (currentHosts().length === 0) {
+                  e.preventDefault();
+                  wrap.classList.add('is-invalid');
+                  textInput.focus();
+                } else {
+                  wrap.classList.remove('is-invalid');
+                }
               });
             })();
           </script>
diff --git a/templates/edit.html b/templates/edit.html
deleted file mode 100644
index dde2d00..0000000
--- a/templates/edit.html
+++ /dev/null
@@ -1,36 +0,0 @@
-{% extends "base.html" %}
-
-{% block title %}Edit {{ filename }} - Caddy Manager{% endblock %}
-{% block page_pretitle %}Caddy{% endblock %}
-{% block page_title %}Edit Site{% endblock %}
-
-{% block header_actions %}
-{% endblock %}
-
-<!---->
-
-{% block content %}
-<div class="row">
-  <div class="col">
-    <div class="card">
-      <div class="card-header">
-        <h3 class="card-title">Editing <span class="badge bg-lt font-monospace">{{ filename }}</span></h3>
-      </div>
-      <div class="card-body">
-        <form method="post">
-          <div class="mb-3">
-            <textarea name="content" class="form-control" rows="18" style="font-family:var(--tblr-font-monospace, monospace)">{{ content }}</textarea>
-          </div>
-          <div class="form-footer d-flex gap-2">
-            <button type="submit" class="btn btn-primary">
-              Save Changes</button>
-            <a href="{{ url_for('sites') }}" class="btn">
-              Cancel
-            </a>
-          </div>
-        </form>
-      </div>
-    </div>
-  </div>
-</div>
-{% endblock %}
diff --git a/templates/sites.html b/templates/sites.html
index 2434438..59727e0 100644
--- a/templates/sites.html
+++ b/templates/sites.html
@@ -247,7 +247,15 @@
                 <span class="badge bg-primary-lt" title="{{ b.hosts[1:] | join(', ') }}">+{{ b.hosts | length - 1 }}</span>
               {% endif %}
             {% else %}
-              —
+              <span class="text-danger d-inline-flex align-items-center gap-1" title="No host configured">
+                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="icon icons-tabler-outline icon-tabler-alert-triangle">
+                  <path stroke="none" d="M0 0h24v24H0z" fill="none" />
+                  <path d="M12 9v4" />
+                  <path d="M10.363 3.591l-8.106 13.534a1.914 1.914 0 0 0 1.636 2.871h16.214a1.914 1.914 0 0 0 1.636 -2.87l-8.106 -13.536a1.914 1.914 0 0 0 -3.274 0" />
+                  <path d="M12 16h.01" />
+                </svg>
+                No host
+              </span>
             {% endif %}
           </td>
           <!-- END HOST CELL -->
-- 
