diff --git a/templates/site_blocks.html b/templates/site_blocks.html
index 09424f6..9778057 100644
--- a/templates/site_blocks.html
+++ b/templates/site_blocks.html
@@ -229,7 +229,7 @@
           <!-- BEGIN SITE ADDRESS CELL -->
           <td>
             {% if b.site_addresses %}
-              {{ b.site_addresses[0] }}
+              <span class="d-inline-block text-truncate align-bottom" style="max-width: 280px;" title="{{ b.site_addresses[0] }}">{{ b.site_addresses[0] }}</span>
               {% if b.site_addresses | length > 1 %}
                 <span class="badge bg-{{ row_color }}-lt" title="{{ b.site_addresses[1:] | join(', ') }}">+{{ b.site_addresses | length - 1 }}</span>
               {% endif %}
@@ -249,16 +249,16 @@
           <!-- BEGIN UPSTREAM CELL -->
           <td>
             {% if b.type == 'reverse_proxy' %}
-              <span class="font-monospace">{{ b.host or '-' }}</span>
+              <span class="d-inline-block text-truncate align-bottom font-monospace" style="max-width: 200px;" title="{{ b.host or '-' }}">{{ b.host or '-' }}</span>
                {% if b.port %}<span class="badge {{ 'bg-muted-lt' if b.disabled else 'bg-' + row_color + '-lt' }} font-monospace">{{ b.port }}</span>{% endif %}
             {% elif b.type == 'redirect' %}
-              <span class="font-monospace">{{ b.target or '-' }}</span>
+              <span class="d-inline-block text-truncate align-bottom font-monospace" style="max-width: 200px;" title="{{ b.target or '-' }}">{{ b.target or '-' }}</span>
               {% if b.redirect_code %}<span class="badge {{ 'bg-muted-lt' if b.disabled else 'bg-' + row_color + '-lt' }} font-monospace">{{ b.redirect_code }}</span>{% endif %}
             {% elif b.type == 'load_balancer' %}
               <span class="font-monospace">{{ b.upstreams | length }} upstreams</span>
               {% if b.lb_policy %}<span class="badge {{ 'bg-muted-lt' if b.disabled else 'bg-' + row_color + '-lt' }} font-monospace">{{ b.lb_policy }}</span>{% endif %}
             {% elif b.type == 'static_site' %}
-              <span class="font-monospace">{{ b.path or '-' }}</span>
+              <span class="d-inline-block text-truncate align-bottom font-monospace" style="max-width: 200px;" title="{{ b.path or '-' }}">{{ b.path or '-' }}</span>
               {% if b.encode %}<span class="badge {{ 'bg-muted-lt' if b.disabled else 'bg-' + row_color + '-lt' }} font-monospace">{{ b.encode | join(', ') }}</span>{% endif %}
             {% else %}
               <span class="font-monospace">custom</span>
